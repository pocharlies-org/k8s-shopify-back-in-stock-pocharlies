apiVersion: v1
kind: ConfigMap
metadata:
  name: back-in-stock-maddy-config
  labels:
    app.kubernetes.io/name: back-in-stock-maddy
data:
  maddy.conf: |
    $(hostname) = {env:MADDY_HOSTNAME}
    $(primary_domain) = {env:MADDY_DOMAIN}

    tls off

    hostname $(hostname)
    state_dir /data
    runtime_dir /tmp

    smtp tcp://0.0.0.0:25 {
        limits {
            all rate 50 1m
            all concurrency 10
        }

        source $(primary_domain) {
            deliver_to &remote_queue
        }

        default_source {
            deliver_to &remote_queue
        }
    }

    target.queue remote_queue {
        target &outbound_delivery
        autogenerated_msg_domain $(primary_domain)
    }

    target.remote outbound_delivery {
        limits {
            destination rate 20 1s
            destination concurrency 10
        }
        mx_auth {
            dane
            mtasts {
                cache fs
                fs_dir mtasts_cache/
            }
            local_policy {
                min_tls_level encrypted
                min_mx_level none
            }
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-app
  labels:
    app.kubernetes.io/name: back-in-stock-app
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: back-in-stock-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back-in-stock-app
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      imagePullSecrets:
        - name: harbor-pull
      nodeSelector:
        role: edge
        kubernetes.io/arch: amd64
      tolerations:
        - key: role
          operator: Equal
          value: edge
          effect: NoSchedule
      containers:
        - name: app
          image: harbor.lan.e-dani.com/homelab/shopify-back-in-stock@sha256:c10ec96f6d1a35c73f5a4cf914cb824339187a2e2d59d1c3ad3b0fcbaa0a6231
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3470
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3470"
            - name: SHOPIFY_APP_URL
              value: https://sauvage.e-dani.com
            - name: CALLBACK_BASE_URL
              value: https://sauvage.e-dani.com
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: shared-postgres-app
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shared-postgres-app
                  key: password
            - name: DATABASE_URL
              value: postgresql://$(DB_USER):$(DB_PASSWORD)@postgres-shared-rw.databases.svc.cluster.local:5432/back_in_stock
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBITMQ_URL
              value: amqp://$(RABBITMQ_DEFAULT_USER):$(RABBITMQ_DEFAULT_PASS)@shared-rabbitmq.databases.svc.cluster.local:5672/%2Fback-in-stock
          envFrom:
            - secretRef:
                name: back-in-stock-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 75m
              memory: 256Mi
            limits:
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: back-in-stock-app
  labels:
    app.kubernetes.io/name: back-in-stock-app
spec:
  selector:
    app.kubernetes.io/name: back-in-stock-app
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-worker-email
  labels:
    app.kubernetes.io/name: back-in-stock-worker-email
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: back-in-stock-worker-email
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back-in-stock-worker-email
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      imagePullSecrets:
        - name: harbor-pull
      nodeSelector:
        role: edge
        kubernetes.io/arch: amd64
      tolerations:
        - key: role
          operator: Equal
          value: edge
          effect: NoSchedule
      containers:
        - name: worker
          image: harbor.lan.e-dani.com/homelab/shopify-back-in-stock-worker-email@sha256:511ebc11ddd31656ae7465d2f40f71bad3e7ee1946586e86f08f1b9baa732e75
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: QUEUE_NAME
              value: notifications.email
            - name: HEALTH_PORT
              value: "8080"
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBITMQ_URL
              value: amqp://$(RABBITMQ_DEFAULT_USER):$(RABBITMQ_DEFAULT_PASS)@shared-rabbitmq.databases.svc.cluster.local:5672/%2Fback-in-stock
          envFrom:
            - secretRef:
                name: back-in-stock-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: back-in-stock-worker-email
  labels:
    app.kubernetes.io/name: back-in-stock-worker-email
spec:
  selector:
    app.kubernetes.io/name: back-in-stock-worker-email
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-worker-whatsapp
  labels:
    app.kubernetes.io/name: back-in-stock-worker-whatsapp
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: back-in-stock-worker-whatsapp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back-in-stock-worker-whatsapp
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      imagePullSecrets:
        - name: harbor-pull
      nodeSelector:
        role: edge
        kubernetes.io/arch: amd64
      tolerations:
        - key: role
          operator: Equal
          value: edge
          effect: NoSchedule
      containers:
        - name: worker
          image: harbor.lan.e-dani.com/homelab/shopify-back-in-stock-worker-whatsapp@sha256:57f669aba77faecda5bac8e77d658d3add73c55c7b786064f56c30792fc1364a
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: QUEUE_NAME
              value: notifications.whatsapp
            - name: HEALTH_PORT
              value: "8080"
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBITMQ_URL
              value: amqp://$(RABBITMQ_DEFAULT_USER):$(RABBITMQ_DEFAULT_PASS)@shared-rabbitmq.databases.svc.cluster.local:5672/%2Fback-in-stock
          envFrom:
            - secretRef:
                name: back-in-stock-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: back-in-stock-worker-whatsapp
  labels:
    app.kubernetes.io/name: back-in-stock-worker-whatsapp
spec:
  selector:
    app.kubernetes.io/name: back-in-stock-worker-whatsapp
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-worker-telegram
  labels:
    app.kubernetes.io/name: back-in-stock-worker-telegram
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: back-in-stock-worker-telegram
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back-in-stock-worker-telegram
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      imagePullSecrets:
        - name: harbor-pull
      nodeSelector:
        role: edge
        kubernetes.io/arch: amd64
      tolerations:
        - key: role
          operator: Equal
          value: edge
          effect: NoSchedule
      containers:
        - name: worker
          image: harbor.lan.e-dani.com/homelab/shopify-back-in-stock-worker-telegram@sha256:3cfef259e6e00004ce54d9fd308397dcab83e6bfd06665b12f873d74c508aee5
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: QUEUE_NAME
              value: notifications.telegram
            - name: HEALTH_PORT
              value: "8080"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: shared-postgres-app
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shared-postgres-app
                  key: password
            - name: DATABASE_URL
              value: postgresql://$(DB_USER):$(DB_PASSWORD)@postgres-shared-rw.databases.svc.cluster.local:5432/back_in_stock
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secrets
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBITMQ_URL
              value: amqp://$(RABBITMQ_DEFAULT_USER):$(RABBITMQ_DEFAULT_PASS)@shared-rabbitmq.databases.svc.cluster.local:5672/%2Fback-in-stock
          envFrom:
            - secretRef:
                name: back-in-stock-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: back-in-stock-worker-telegram
  labels:
    app.kubernetes.io/name: back-in-stock-worker-telegram
spec:
  selector:
    app.kubernetes.io/name: back-in-stock-worker-telegram
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-maddy
  labels:
    app.kubernetes.io/name: back-in-stock-maddy
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: back-in-stock-maddy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back-in-stock-maddy
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        role: edge
        kubernetes.io/arch: amd64
      tolerations:
        - key: role
          operator: Equal
          value: edge
          effect: NoSchedule
      containers:
        - name: maddy
          image: foxcpp/maddy@sha256:de42151adff6388edb5e4ee88f60334fa1ab85e309485193ecb1c2db20203315
          imagePullPolicy: IfNotPresent
          ports:
            - name: smtp
              containerPort: 25
          env:
            - name: MADDY_HOSTNAME
              value: mail.skirmshop.es
            - name: MADDY_DOMAIN
              value: skirmshop.es
          volumeMounts:
            - name: config
              mountPath: /data/maddy.conf
              subPath: maddy.conf
              readOnly: true
            - name: data
              mountPath: /data
          readinessProbe:
            tcpSocket:
              port: smtp
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 256Mi
      volumes:
        - name: config
          configMap:
            name: back-in-stock-maddy-config
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: bis-maddy
  labels:
    app.kubernetes.io/name: back-in-stock-maddy
spec:
  selector:
    app.kubernetes.io/name: back-in-stock-maddy
  ports:
    - name: smtp
      port: 25
      targetPort: smtp
